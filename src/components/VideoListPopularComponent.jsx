import React from 'react';

const VideoListPopularComponent = ({apiConfig}) => {
  return (
    <div style={{color: '#fff'}}>
      VideoList New & Popular
    </div>
  );
}

export default VideoListPopularComponent;

